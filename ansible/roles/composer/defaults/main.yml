---
# Composer role default variables

# Composer installation directory
composer_install_dir: /usr/local/bin

# Composer binary name
composer_binary_name: composer

# Composer version (use 'latest' for latest stable)
composer_version: latest

# Temporary directory for downloads
composer_temp_dir: /tmp/composer-setup
